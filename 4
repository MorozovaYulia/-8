#include <iostream> 
#include <string> 
using namespace std; 
#include <math.h> 
int main() 

{ 
  int xa, ya; 
  cout << "enter the X coordinate of point A \n" ; 
  cin >> xa ; 
  cout << "enter the Y coordinate of point A \n" ; 
  cin  >> ya; 
  if (xa>0) 
    if (ya>0) 
    cout << "First quarter"; 
    else  
    cout << "Fourth quarter"; 
else 
if  (ya>0) 
    cout << "Second quarter; 
    else  
    cout << "Third quarter"; 
} 
