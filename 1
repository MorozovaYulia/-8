#include <iostream> 
#include <string> 
using namespace std; 
int main() 
{ 
  int a,b; 
  cout << "enter the variable value \n"; 
  cin >> a >>b; 
 if (a!=b) 
        if (a>b) 
      b=a; 
        else 
     a=b; 
else (a=b=0) ; 
cout << a <<b; 
} 
